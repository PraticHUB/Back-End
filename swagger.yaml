openapi: 3.0.0
info:
  title: API de Exemplo
  description: API simples com Node.js e Swagger
  version: 1.0.0
paths:
  /:
    get:
      summary: Retorna uma mensagem de olá
      responses:
        '200':
          description: Mensagem de olá
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Olá, mundo!"
  /hello:
    get:
      summary: Retorna uma mensagem personalizada
      responses:
        '200':
          description: Mensagem personalizada
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Olá do servidor Node.js!"
